bases:
  - "../../../commons/general.yaml"

releases:
- name: cert-manager-webhook-pdns
  labels:
    parent: cert-manager
  namespace: cert-manager
  chart: ../../../charts/cert-manager-webhook-pdns
  values:
  - "values.yaml"