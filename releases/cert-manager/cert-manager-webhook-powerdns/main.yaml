bases:
  - "../../../commons/general.yaml"

repositories:
  - name: cert-manager-webhook-powerdns
    url: https://lordofsystem.github.io/cert-manager-webhook-powerdns

releases:
- name: cert-manager-webhook-powerdns
  labels:
    parent: cert-manager
  namespace: cert-manager
  chart: cert-manager-webhook-powerdns/cert-manager-webhook-powerdns
  values:
  - "values.yaml"