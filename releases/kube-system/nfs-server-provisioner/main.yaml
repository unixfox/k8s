bases:
  - "../../../commons/repos.yaml"
  - "../../../commons/general.yaml"

releases:
- name: nfs-server-provisioner-oraclefree3
  labels:
    parent: kube-system
  namespace: kube-system
  chart: kvaps/nfs-server-provisioner
  values:
  - "values.yaml"