bases:
  - "../../../commons/general.yaml"

releases:
- name: nitter-cron-restart-deployment
  labels:
    parent: nitter
  namespace: nitter
  chart: ../../../charts/cron-restart-deployment
  values:
  - "values.yaml"
